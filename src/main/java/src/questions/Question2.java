package src.questions;

public class Question2 {
    /*
        The impact of accessibility will be if a instance member change to private, because private members is only visible inside the class
        If is public will be visible to all classes
     */
}
