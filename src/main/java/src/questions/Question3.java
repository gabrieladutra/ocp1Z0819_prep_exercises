package src.questions;

public class Question3 {
    /* The code is not well encapsulated, because have public members.
    That violates the principle that properties need to be accessed by its methods.
    getMethod must be public;
    */

}
